{
  "name": "@byteroute/seed",
  "private": true,
  "version": "1.2.0",
  "type": "module",
  "scripts": {
    "dev": "node -e \"console.log('seed: dev (use pnpm seed)')\"",
    "seed": "tsx src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "lint": "node -e \"console.log('seed: lint (placeholder)')\"",
    "test": "node -e \"console.log('seed: test (placeholder)')\""
  },
  "dependencies": {
    "@byteroute/shared": "workspace:*",
    "mongoose": "^9.0.0"
  },
  "devDependencies": {
    "@byteroute/typescript": "workspace:*",
    "@types/node": "^24.10.9",
    "tsx": "^4.20.0",
    "typescript": "^5.7.3"
  }
}
