# Dashboard (Vite)

# Optional: override the Socket.IO base URL.
# If unset, the dashboard connects to the same origin (recommended with the dev proxy and Docker Compose).
#
# Examples:
# VITE_SOCKET_URL=http://localhost:4000
# VITE_SOCKET_URL=http://localhost:8080
VITE_SOCKET_URL=
